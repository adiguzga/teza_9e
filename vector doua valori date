# include<iostream>
# include<fstream>
using namespace std;

ifstream fin("int.txt");
ofstream fout("out.txt");

void citire(int a[100], int &n)
{
    fin>>n;
    for(int i=0;i<n;i++)
        fin>>a[i];
}

void afisare(int a[100], int &n, int k1, int k2, int b[100], int &m)
{
    for(int i=0;i<n;i++)
        if (a[i]>=k1&&a[i]<=k2)
        {
            b[m]=a[i];
            m++;
        }
    for(int i=0;i<m;i++)
        fout<<b[i]<<" ";
}

int main()
{
    int a[100], n, k1, k2, b[100], m=0;
    cin>>k1;
    cin>>k2;
    citire(a,n);
    afisare(a,n,k1,k2,b,m);//cerinta era ca se dau niste numere si trebuie sa le bagi pe numerele aflate intre valorile alea de la tastatura intr-un nou vector


	return 0;
}
